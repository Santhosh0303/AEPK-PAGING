[project]
name = "aepk-paging"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = [
  "numpy==2.4.6",
  "pytest==9.1.1",
  "hypothesis==6.155.7",
  "galois==0.4.11",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
